- set_meta_tags  title: '紹介可能人材一覧|ENGIST'
- set_meta_tags  description: ''

- set_meta_tags noindex: true

.row
  br
  br
  table.company
   colgroup
     col width="10%"
     col width="30%"  
     col width="10%"
     col width="10%"
     col width="10%"
     col width="10%" 
     col width="10%"
     col width="10%"
   tr
     th 年齢
     th 言語
     th プログラマー歴
     th 国籍
     th エンジニアレベル
     th 日本語レベル
     th 稼働希望日
     th 詳細
   - @works.each do |work|
    tr
     td = work.age
     td = work.program_python
     td 
          - if work.program_histroy.to_i >= 3
            strong style="color: red;" = work.program_histroy
          - else
            = work.program_histroy
     td = work.nationality
     td           
          - if ['即戦力', '経験者'].include?(work.engineer_level)
            strong style="color: blue;" = work.engineer_level
          - else
            = work.engineer_level
     td 
          - if ['日常会話', '日本人同等'].include?(work.japanese_level)
            strong style="color: green;" = work.japanese_level
          - else
            = work.japanese_level
     td = work.period
     td 
      - if admin_signed_in?
       = link_to work.name, work_path(work), clsss:"store"
      - else
       = link_to "詳細へ進む", work_path(work), clsss:"store"
  br
    hr.line
    br
    = paginate(@works)
